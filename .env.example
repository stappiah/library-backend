# Copy to .env and fill values.
# Used by django-decouple via `config("..." )`.

# Required
SECRET_KEY=change-me

# Optional (used to override the default sqlite DB)
# Example: DATABASE_URL=postgres://user:pass@host:5432/dbname
DATABASE_URL=

